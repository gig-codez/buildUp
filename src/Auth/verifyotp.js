const otpModel = require("../models/otp.model");
class VerifyOtp {
  static async verify(req, res) {
    try {
        const response=
    } catch (error) {
      res.status(400).json({ message: error });
    }
  }
}
